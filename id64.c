int id62();


void id64(){
  int x = id62(), y = id62(), z = id62();
  if (y > 0) {
    do {
      if (id62()) {
        x = x + y;
      } else {
        z = x - y;
      }
    } while (x < y && y < z);
  }
}